namespace NyxGui;

/// <summary>Logical keys routed from the host into focused widgets (e.g. <see cref="INyxTextEntry"/>).</summary>
public enum NyxGuiKey
{
    None,
    Backspace,
    Delete,
    Left,
    Right,
    Up,
    Down,
    Home,
    End,
    Enter,
    Escape,
    Tab,
}
